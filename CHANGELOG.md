# 版本记录

## 仓库维护（v1.11.0 后）

- 修正发布脚本对 `RELEASE_TAG` 前缀的归一化处理，Release 标签带不带 `v` 前缀均可通过校验（PR #1）。
- 补充 MIT LICENSE，并在素材说明中记录 `app-icon-v2.png`。
- 以上为仓库与文档维护，未修改游戏运行逻辑，不提升游戏版本。

## 1.11.0

- 按 core、data、state、systems、rendering、ui 拆分源码，保留原有游戏规则、剧情和存档格式。
- 使用 esbuild 将 ES Modules 构建为 ES2017 / Chrome 61 目标的离线经典脚本。
- 新增固定四文件白名单、资源路径、语法、禁用能力、ZIP 内容及体积校验；生成可重复 ZIP 和 SHA-256。
- 新增 8 项规则/存档/产物测试，保留完整状态机与 DOM/Canvas 模拟回归。
- 新增 GitHub Actions：推送与 PR 构建测试，发布同版本 Release 后上传小红书 ZIP 与校验文件。
- 更新开发命令、目录职责和 AGENTS.md；未进行手机真机或 Chrome 61 实测。

## 仓库初始化（基于 1.10.1）

- 纳入完整游戏源码、PNG 精灵图集、图标与回归测试。
- 纳入小红书 `minitool-zip-builder` 1.6.0 技能，保留原始参考文档与 Node/Python 审计脚本。
- 新增 `AGENTS.md`，记录项目入口、玩法约定、存档兼容性、测试和离线包交付规则。
- 新增 `.editorconfig` 与 `.gitignore`，补充 README 的运行、技能使用及打包说明。
- 本次仓库整理不修改游戏运行逻辑，不提高游戏版本。

## 1.10.1

### 修复

- 战斗预告纳入进入房间时的本步毒伤，与实际结算共用预测逻辑；区分原地伏击和一击必杀的结算方式。
- 事件时间流达到 200 条上限后仍更新最新日志与未读提示。
- 正在展示的未读故事独立保存，恢复时保持原文，不重新抽取候选故事；确认继续后才标记完成。
- 保持剧情 30 步冷却起点、优先队列和旧存档兼容。

### 验证覆盖

- 增加 10 组不补血的战斗边界用例、405 条日志刷新和未读故事中断恢复回归。
- 保留地图连通性、随机事件、道具、寻路、剧情限频和结局等状态机测试。
- 状态机检查不等同于手机或 Chrome 61 的视觉、兼容性和性能实测。
