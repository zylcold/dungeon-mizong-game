# 版本记录

## 1.13.2

- 修复故事层「继续」/分叉按钮点不动：`.story-vignette` 设为 `pointer-events: none`，卡片与按钮抬高层级。
- 「继续」与 ChoiceBar 热区 ≥44px，并补充 `:active` 反馈。

## 1.13.1

- 任意两次演出（主线 + lore）全局至少间隔 10 步；同一步不连弹；`M0`/`E` 豁免；`ChoiceBar→echo` 同次交互可紧接。
- DualPanel 移除「幻觉 / 现实」标签，仅保留暖红 / 冷青版式与错位淡入。
- 分叉按钮文案改为「追钟声更深」「循犬吠寻路」，界面提示不再出现「幻觉」「现实」。

## 1.13.0

- 故事与演出专版：主线 M0–M4 / F1 / F2（坠谷→幻境→苏醒），填充 lore 不挡主线。
- 新增三种演出形态：`StoryCard`（章节卡）、`DualPanel`（幻/现实双栏）、`ChoiceBar`（分叉按钮 + 回响）。
- 存档字段 `mainBeat` / `branchFlags` / `realityAnchors`；旧档缺字段安全接入，不编造历史分叉。
- 主线走 `special` 通道，不受普通 20 步冷却影响；F1 后连打收束高潮。
- 现实锚点（`event:echo` / `item:potion` / `event:cache`）可解锁 F2 第三选项；结局文案随 `branchFlags` 变体。
- 文本量验收以产品定稿为准；**玩法数值（迷宫/战斗/事件/道具）未改**。

## 1.12.0

- 新增开发者日记：检测到版本更新后首次进入自动展示本版更新内容，开始界面可随时回看；已读版本独立保存在 `dungeon-mizong-diary-v1`，不影响游戏存档。
- `src/config.js` 新增 `APP_VERSION` 作为游戏内版本唯一来源，测试校验其与 package.json 及开发者日记首条一致。
- 新增日记自动展示、去重与回看的 DOM 回归及版本同步测试。
- 剧情系统重构：触发即演或跳过，移除持久化演出队列与优先级排序；被跳过的片段不标记已读，等下次同类触发再走完整逻辑（PR #6）。
- 普通演出最小间隔调整为 20 步；同一瞬间主动作（击杀/事件结算）优先演出，血量记忆线只在创下新低且不抢戏时播放；序章后新增 20 步演出呼吸期（PR #4/#8）。
- 修复演出延迟补播：血量线改为仅创新低时尝试一次；事件面板直接关闭时当场清空待演剧情，不再残留到后续结算点；旧档队列字段自动清理（PR #7）。
- 新增 `GAMEPLAY.md` 玩法规则手册与仓库内玩法技能（PR #5）。

## 仓库维护（v1.11.0 后）

- 修正发布脚本对 `RELEASE_TAG` 前缀的归一化处理，Release 标签带不带 `v` 前缀均可通过校验（PR #1）。
- 补充 MIT LICENSE，并在素材说明中记录 `app-icon-v2.png`。
- 以上为仓库与文档维护，未修改游戏运行逻辑，不提升游戏版本。

## 1.11.0

- 按 core、data、state、systems、rendering、ui 拆分源码，保留原有游戏规则、剧情和存档格式。
- 使用 esbuild 将 ES Modules 构建为 ES2017 / Chrome 61 目标的离线经典脚本。
- 新增固定四文件白名单、资源路径、语法、禁用能力、ZIP 内容及体积校验；生成可重复 ZIP 和 SHA-256。
- 新增 8 项规则/存档/产物测试，保留完整状态机与 DOM/Canvas 模拟回归。
- 新增 GitHub Actions：推送与 PR 构建测试，发布同版本 Release 后上传小红书 ZIP 与校验文件。
- 更新开发命令、目录职责和 AGENTS.md；未进行手机真机或 Chrome 61 实测。

## 仓库初始化（基于 1.10.1）

- 纳入完整游戏源码、PNG 精灵图集、图标与回归测试。
- 纳入小红书 `minitool-zip-builder` 1.6.0 技能，保留原始参考文档与 Node/Python 审计脚本。
- 新增 `AGENTS.md`，记录项目入口、玩法约定、存档兼容性、测试和离线包交付规则。
- 新增 `.editorconfig` 与 `.gitignore`，补充 README 的运行、技能使用及打包说明。
- 本次仓库整理不修改游戏运行逻辑，不提高游戏版本。

## 1.10.1

### 修复

- 战斗预告纳入进入房间时的本步毒伤，与实际结算共用预测逻辑；区分原地伏击和一击必杀的结算方式。
- 事件时间流达到 200 条上限后仍更新最新日志与未读提示。
- 正在展示的未读故事独立保存，恢复时保持原文，不重新抽取候选故事；确认继续后才标记完成。
- 保持剧情 30 步冷却起点、优先队列和旧存档兼容。

### 验证覆盖

- 增加 10 组不补血的战斗边界用例、405 条日志刷新和未读故事中断恢复回归。
- 保留地图连通性、随机事件、道具、寻路、剧情限频和结局等状态机测试。
- 状态机检查不等同于手机或 Chrome 61 的视觉、兼容性和性能实测。
