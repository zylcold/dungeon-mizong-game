# 玩法规则手册

本文完整描述「地下城：迷踪」的实际玩法规则，以 `src/` 实现为准，数值常量集中在 `src/config.js`。调整任何规则时，必须同步更新本文、`src/config.js` 与 `tests/smoke.test.cjs` 对应断言。

## 1. 核心循环

单局 = 单层迷宫，无二周目。玩家在迷宫中心醒来（HP 100/100），四向探索、战斗、触发事件、收集道具；走到任一出口选择离开即生存结局，HP 归零（战斗或毒素）即坠落结局。两种结局都会播放「梦醒」叙事，随后展示完整迷宫、本局路线与统计。

## 2. 迷宫生成

| 规则 | 数值 | 出处 |
| --- | --- | --- |
| 尺寸 | 135×135 = 18225 房间，单层 | `MAZE_SIZE` |
| 生成算法 | Growing Tree，每图随机 newestBias 0.42–0.82 混合「最新前沿/随机前沿」 | `core/maze.js` |
| 环路 | 额外破墙 18%–28%，优先消除死胡同 | 同上 |
| 出生点 | 迷宫中心 | 同上 |
| 出口 | 最多 3 个，位于外围，相互曼哈顿距离 ≥ ⌊135×0.62⌋ | 同上 |
| 出口安全区 | 出口周围曼哈顿 ≤2 格 + 中心到出口最短路最后 4 房间，不放置任何实体 | `EXIT_SAFE_RADIUS` / `EXIT_SAFE_PATH_ROOMS` |
| 出生安全区 | 距中心曼哈顿 ≤2 不放实体 | `core/maze.js` |

实体分布（同一打乱序列依次占位，互不重叠）：

| 类型 | 数量 | 权重 |
| --- | --- | --- |
| 敌人 | ⌊18225 × 4.5%–6%⌋ | 地穴鼠 7 / 骷髅守卫 7 / 毒史莱姆 5 / 游荡幽灵 4 / 宝箱怪 3 |
| 事件 | ⌊18225 × 3.5%⌋ ≈ 637 | 宝箱 7 / 生命泉 5 / 陷阱 6 / 地图 4 / 祭坛 4 / 遗骸 5 / 浓雾 4 / 法阵 3 / 石门 3 / 裂隙 4 / 回声 4 / 农具 4 |
| 道具 | ⌊18225 × 1.2%⌋ ≈ 218 | 药剂 4 / 视野 3 / 必杀 2 / 瞬移 2 |

注：`catalog.js` 中的「出口守卫」（35–55 HP）当前生成器不投放，仅作为定义保留。

## 3. 移动、步数与逐步效果

- 四向移动，墙不可穿越；每次成功移动 `totalSteps +1`，瞬移与传送法阵同样计步。
- 每步结算顺序（`tickEffects`）：视野 buff → 浓雾 → 出口线索递减 → 毒素扣血（2 HP/步，归零即终局）。
- 「暂时离开」的事件格会记入 `dismissedKey`，玩家走出该格后再次进入可重新触发；同一步内不重复弹。
- 自动寻路：BFS 仅限已探索连通区域，95ms/步；遇敌人、事件、剧情或任何弹层立即停止。
- 输入：手机滑动、桌面键盘、点击主画面/放大地图中已探索格自动寻路。

## 4. 战斗（血量比较制）

核心规则：敌方不攻击。**玩家当前 HP 严格大于敌方 HP 即胜**，胜后扣除与敌方 HP 等值的生命；不大于（含相等）则本局直接结束。

| 敌人 | HP | 特性 |
| --- | --- | --- |
| 地穴鼠 | 6–12 | 无 |
| 骷髅守卫 | 15–25 | 无 |
| 毒史莱姆 | 12–20 | 击败后中毒 3 步（与现有毒取较大值），每步 -2 HP |
| 游荡幽灵 | 18–28 | 靠近 1 格才显形 |
| 宝箱怪 | 22–35 | 靠近才显形，外观同宝箱 |

- 非伏击获胜：进入目标房间并结算本步毒伤；原地伏击获胜不前进、不结算毒伤。
- 一击必杀：0 伤害、0 中毒、不前进，仅遇敌面板可用。
- 击败敌人 28% 概率原地掉落可立即开启的宝箱（`ENEMY_CHEST_DROP_RATE`）。
- 遇敌预告会完整预测剩余 HP、本步毒伤与致死风险，预测与结算共用同一纯函数。

## 5. 随机事件（12 类）

所有概率均由 `eventRoll(key, salt)` 以地图种子确定性掷出：同局同位置同结果。

| 事件 | 交互 | 结果 |
| --- | --- | --- |
| 封尘宝箱 | 选择 | 76% 道具 / 18% 回复 ≤20 / 6% 空 |
| 生命泉 | 选择 | 回复 ≤25 |
| 地刺陷阱 | 即时 | 受 8–15 伤（保底 1 HP），陷阱消失 |
| 地图残片 | 即时 | 出口箭头强化 30 步 |
| 古老祭坛 | 选择 | HP>12 可献祭：-12 HP、上限 +15（净 +3） |
| 冒险者遗骸 | 选择 | 66% 道具；否则原地伏击（>0.90 幽灵，否则地穴鼠） |
| 浓雾区域 | 即时 | 视野降为 1 格，持续 15 步 |
| 传送法阵 | 选择 | 随机瞬移（计 1 步，路线图虚线标记） |
| 神秘石门 | 选择 | 打通一堵封闭墙形成捷径；无候选墙时给出口线索 20 步 |
| 盘根裂隙 | 选择 | 48% 受 5–12 伤 / 34% 道具 / 18% 出口线索 12 步 |
| 谷底回声 | 即时 | 出口线索 16 步 |
| 遗落农具 | 选择 | 56% 道具 / 26% 回复 ≤14 / 18% 记忆文案 |

## 6. 道具

| 道具 | 效果 |
| --- | --- |
| 恢复药剂 | 回复 ≤30，满血禁用 |
| 视野拓宽 | 关闭主画面迷雾、地图缩小 50%，+30 步；可叠加至上限 60 步 |
| 一击必杀 | 遇敌面板内直接击杀，见第 4 节 |
| 瞬移卷轴 | 传送到安全位置（距玩家 ≥6、非出口、无实体），计 1 步 |

宝箱/事件掉落权重（`randomItemFor`）：药剂 38% / 视野 28% / 必杀 17% / 瞬移 17%。地图直落权重见第 2 节。

## 7. 视野与迷雾

- 视野按**连通房间 BFS** 计（非欧氏距离）：默认 2 格、浓雾 1 格。
- 视野道具期间视野无限，但永久探索记录仍只写入玩家周围 2 格（性能与缩略图一致性）。
- `explored` 永久累积；缩略图只绘制已探索网络与出口方向箭头（最多 3 个）。

## 8. 剧情演出系统

**即时演出，无队列**：剧情在触发点当场尝试演出；条件不满足（间隔不足 / 同一步已演过 / 面板未关闭 / 已有未读演出）时**直接跳过并丢弃**，不排队、不缓存。被跳过的片段不标记已读，等下次同类触发（下次击败同类怪物、遇到同类事件、拾取同类道具、下次界面更新的血量下一次创新低）重新走完整判定。

**触发时机**：怪物=首次击败该类怪物后（战斗面板关闭时）；道具=物品实际入包后（使用初始物品不触发，事件结算内获得的道具延迟到面板关闭后尝试）；事件=结算完成或离开/直接关闭面板时（即时类事件如陷阱/地图残片/浓雾/回声在当场结算后立即尝试）；血量记忆=HP 创下新低且首次跌破 75%/50%/25% 阈值的那一刻尝试一次（`hpStoryRatioLow` 记录历史最低，跳过即弃、等下一次新低）。同局同类只演一次（`loreSeen` / `storyScenes`，仅在真正演出后标记）。

**演出间隔（普通剧情）**：两次普通演出至少相隔 20 步（`NORMAL_STORY_MIN_GAP_STEPS`）；同一步绝不连弹；序章/结局豁免。无队列即无优先级排序，同一步的多个触发只有第一个满足条件的演出，其余按跳过规则处理、留待各自下次触发。

**未读演出恢复**：当前未读演出单独持久化（`currentStory`），继续游戏恢复原文，不重新抽签、不重置间隔起点；确认「继续」后才标记完成。

**文本变体**：每个演出从 3–5 条候选中抽取（variants + 幻象/真实 + 段落组合 + 补齐文案），单次演出 ≤3 段，抽取种子 = 房间 + 场景 + 当前步数。

**氛围日志**：每 8–16 步随机输出一条环境文案（`AMBIENT_COPY` 池）。

## 9. 叙事结构

幻觉/现实双线：每个故事对象含 `illusion` / `reality` 两版文本，演出不显示路线标签。血量记忆残片（75/50/25）逐步披露身份与坠谷经过；两种结局（逃离=天光、死亡=坠落感）均以单段完整文本揭示「迷宫是昏迷幻象、现实是山谷自救」的真相。

## 10. 存档与纪录

| 项 | 值 |
| --- | --- |
| 存档键 | `dungeon-mizong-save-v1`（整份状态 JSON，自动保存） |
| 纪录键 | `dungeon-mizong-records-v1`（Top 20，按步数/探索数排序） |
| 日记键 | `dungeon-mizong-diary-v1`（已读版本，独立于存档） |
| 存档版本 | `SAVE_VERSION = 4`；`STORY_TRIGGER_VERSION = 3` |

旧档兼容：`state/migrations.js` 补齐缺失字段（如 `lastNormalStoryStep` 沿用 `lastStoryStep`，不放宽原节奏）。**新增状态字段必须在此补齐默认值**。

## 11. 数值常量速查

`src/config.js`：`MAZE_SIZE 135` · `ENEMY_DENSITY 4.5%–6%` · `ENEMY_CHEST_DROP_RATE 0.28` · `POISON_DAMAGE_PER_STEP 2` · `SLIME_POISON_DURATION 3` · `VISION_DURATION 30` · `DEFAULT_VISION_RADIUS 2` · `FOG_VISION_RADIUS 1` · `VISION_ZOOM_SCALE 0.5` · `NORMAL_STORY_MIN_GAP_STEPS 20` · `MAX_LOGS 200` · `AUTO_PATH_DELAY 95` · `EXIT_SAFE_RADIUS 2` · `EXIT_SAFE_PATH_ROOMS 4`

## 12. 随机性约定

全游戏使用 `mulberry32` 种子随机；`eventRoll(key, salt) = mulberry32(hash(seed:key:salt))`。**新增任何玩法随机必须走 `eventRoll`**，保证同局确定性、可回放、可测试。瞬移目标点等少数内部随机使用独立派生种子，同样确定性。

## 维护约定

1. 改玩法/数值：`src/config.js` 常量 → `GAMEPLAY.md` 对应小节 → `tests/smoke.test.cjs` 断言 → README「已实现」清单，四处同步。
2. 新增状态字段：`initial-state.js` 初始值 + `migrations.js` 旧档补齐；评估是否需要升 `SAVE_VERSION`。
3. 测试：`npm test`（含构建 + 单元 + 产物 + 冒烟全链路），提交前必须全绿。
